spring:
  jpa:
    hibernate:
      ddl-auto:
        create-drop
  datasource:
    url:
      jdbc:mysql://localhost:3306/livraison_db
    username:
      root
    password:
      V@ste789
    driver-class-name:
      com.mysql.cj.jdbc.Driver

server:
  port: 8090
spring.jpa.show-sql: true